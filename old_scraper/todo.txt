-----TODO-----

1. Getting all job postings, but now we need to improve the quality of the retrieved jobs.
    a. Ensure location is in Vienna
    b. Ensure the job is looking for greater than 3 years of experience
    c. Ensure the job is asking for c# .Net development work
    d. Ensure the salary is greater than 50k euro yearly.
    e. ensure the job is posted within the last week.
    f. failover to other findable jobs after the above criteria are exhausted.





1. Determine how best to load all job postings using ajax request or refactor to use a headless browser.
2. Email filter module for sorting rejections, interview requests, and requests for additional information
3. clean up old files from attachments directory (move to oldfiles directory)
4. Improve sent email to include the necessary information for applications
5. update it to use saad's email address.
6. Add scheduler functionality so that it operates on a time schedule, weekly or daily, etc.
7. Update the hardcoded url in .env, instead using params passed to htpx.get rather than url encoding


-----Architecture-----

1. LowDB for storing job postings (jobs.json)
2. JsonModel.json has model definition
3.



-----done-----
1. Determine how best to get current date of the job posting. --> done(for now)
    a. we can simply sort by date posted to our db, assuming we will always be regularly updating and grabbing all postings.

3. Determine how best to sort postings by date. --> done
4. Determine how best to query lowDB db. --> done
5. Post grabbed urls to text file --> done
6. Send text file as email to Saad --> done